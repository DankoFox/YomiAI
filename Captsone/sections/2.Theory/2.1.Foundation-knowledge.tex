\section{Foundation Knowledge}
\label{sec:foundation}

\subsection{Recommendation Systems and Sequential Modelling}
\label{sec:seq_rec}

A recommendation system aims to predict items that a user is likely to interact with, given their historical behaviour.
The literature mainly studies three broad approaches.
\textit{Collaborative filtering} builds a user item interaction matrix and uses interaction patterns, often through matrix factorization, to learn hidden relationships between users and items.
\textit{Content based filtering} represents items by their attributes (title, genre, description) and retrieves items similar to those a user has previously consumed.
\textit{Hybrid} approaches combine both signals to mitigate the individual weaknesses of each: collaborative methods suffer from the cold start problem when a new item has no interactions, while content based methods ignore the community level purchasing signals that often drive discovery.

Sequential recommendation refines the problem further by treating a user's interactions as an ordered sequence and predicting the \textit{next} item.
The key observation is that a user's near-term intent is more accurately reflected by their most recent interactions than by their full historical profile.
Early sequential methods applied recurrent neural networks (RNNs) to model temporal dynamics, but RNNs encode history into a fixed size state vector, creating an information bottleneck for long sequences.

Self-attention, introduced in the Transformer architecture~\cite{vaswani2017attention}, eliminates this bottleneck by computing a weighted sum over all positions in the sequence for each query position.
The attention weight between positions $i$ and $j$ is:
\begin{equation}
  \alpha_{ij} = \frac{\exp((\mathbf{q}_i \mathbf{k}_j^\top) / \sqrt{d})}{\sum_{l} \exp((\mathbf{q}_i \mathbf{k}_l^\top) / \sqrt{d})}
\end{equation}
where $\mathbf{q}_i$, $\mathbf{k}_j$ are query and key projections of the $i$-th and $j$-th sequence positions, and $d$ is the head dimension.
SASRec~\cite{kang2018self} adapts this to recommendation with a \textit{causal} (left-to-right) mask so that only past interactions influence each prediction, matching the sequential nature of the task.
BERT4Rec~\cite{sun2019bert4rec} instead uses bidirectional attention with masked-item training, achieving higher accuracy but at a greater inference cost.
Figure~\ref{fig:causal_attn} illustrates the causal masking pattern for a four-item sequence.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  >=Stealth,
  every path/.style={line width=0.6pt},
  ibox/.style={draw=blue!45, fill=blue!7, rounded corners=2pt,
               minimum width=0.85cm, minimum height=0.58cm,
               align=center, font=\small},
  obox/.style={draw=orange!55, fill=orange!7, rounded corners=2pt,
               minimum width=0.85cm, minimum height=0.58cm,
               align=center, font=\small},
  lbl/.style={font=\footnotesize, text=gray!75},
]

% ── Grid parameters ──────────────────────────────────────────────────
% Column c (0..3) centre: x = c * 1.35
% Row    r (0..3) centre: y = -r * 1.35
% Cell half-size: 0.48

% ── Fill cells ───────────────────────────────────────────────────────
% Row 0 (q_1): only col 0 allowed
\fill[teal!22]  (-0.48,-0.48) rectangle ( 0.48, 0.48);
\fill[gray!14]  ( 0.87,-0.48) rectangle ( 1.83, 0.48);
\fill[gray!14]  ( 2.22,-0.48) rectangle ( 3.18, 0.48);
\fill[gray!14]  ( 3.57,-0.48) rectangle ( 4.53, 0.48);
% Row 1 (q_2): cols 0,1 allowed
\fill[teal!22]  (-0.48,-1.83) rectangle ( 0.48,-0.87);
\fill[teal!22]  ( 0.87,-1.83) rectangle ( 1.83,-0.87);
\fill[gray!14]  ( 2.22,-1.83) rectangle ( 3.18,-0.87);
\fill[gray!14]  ( 3.57,-1.83) rectangle ( 4.53,-0.87);
% Row 2 (q_3): cols 0,1,2 allowed
\fill[teal!22]  (-0.48,-3.18) rectangle ( 0.48,-2.22);
\fill[teal!22]  ( 0.87,-3.18) rectangle ( 1.83,-2.22);
\fill[teal!22]  ( 2.22,-3.18) rectangle ( 3.18,-2.22);
\fill[gray!14]  ( 3.57,-3.18) rectangle ( 4.53,-2.22);
% Row 3 (q_4): all allowed
\fill[teal!22]  (-0.48,-4.53) rectangle ( 0.48,-3.57);
\fill[teal!22]  ( 0.87,-4.53) rectangle ( 1.83,-3.57);
\fill[teal!22]  ( 2.22,-4.53) rectangle ( 3.18,-3.57);
\fill[teal!22]  ( 3.57,-4.53) rectangle ( 4.53,-3.57);

% ── Cell borders ─────────────────────────────────────────────────────
\foreach \c in {0,1,2,3} {
  \foreach \r in {0,1,2,3} {
    \draw[gray!45] ({-0.48+\c*1.35},{-0.48-\r*1.35})
                rectangle ({0.48+\c*1.35},{ 0.48-\r*1.35});
  }
}

% ── Masked cell labels ────────────────────────────────────────────────
\node[lbl] at (1.35, 0)    {$-\infty$};
\node[lbl] at (2.70, 0)    {$-\infty$};
\node[lbl] at (4.05, 0)    {$-\infty$};
\node[lbl] at (2.70,-1.35) {$-\infty$};
\node[lbl] at (4.05,-1.35) {$-\infty$};
\node[lbl] at (4.05,-2.70) {$-\infty$};

% ── Check marks in allowed cells ─────────────────────────────────────
\foreach \pos in {(0,0),(0,-1.35),(1.35,-1.35),
                  (0,-2.70),(1.35,-2.70),(2.70,-2.70),
                  (0,-4.05),(1.35,-4.05),(2.70,-4.05),(4.05,-4.05)} {
  \node[font=\small, text=teal!60!black] at \pos {$\checkmark$};
}

% ── Input items (above grid, one per column) ──────────────────────────
\node[ibox] (in1) at (0.00, 1.15) {$i_1$};
\node[ibox] (in2) at (1.35, 1.15) {$i_2$};
\node[ibox] (in3) at (2.70, 1.15) {$i_3$};
\node[ibox] (in4) at (4.05, 1.15) {$i_4$};

% Key labels above input items
\node[lbl] at (0.00, 1.78) {$k_1$};
\node[lbl] at (1.35, 1.78) {$k_2$};
\node[lbl] at (2.70, 1.78) {$k_3$};
\node[lbl] at (4.05, 1.78) {$k_4$};

% Arrows: input → top of corresponding column
\draw[->] (in1.south) -- (0.00, 0.48);
\draw[->] (in2.south) -- (1.35, 0.48);
\draw[->] (in3.south) -- (2.70, 0.48);
\draw[->] (in4.south) -- (4.05, 0.48);

% ── Query labels (left of each row) ──────────────────────────────────
\node[lbl, anchor=east] at (-0.72, 0.00)  {$\mathbf{q}_1$};
\node[lbl, anchor=east] at (-0.72,-1.35)  {$\mathbf{q}_2$};
\node[lbl, anchor=east] at (-0.72,-2.70)  {$\mathbf{q}_3$};
\node[lbl, anchor=east] at (-0.72,-4.05)  {$\mathbf{q}_4$};

% ── Output hidden states (right of each row) ─────────────────────────
\node[obox] (o1) at (5.30, 0.00)  {$h_1$};
\node[obox] (o2) at (5.30,-1.35)  {$h_2$};
\node[obox] (o3) at (5.30,-2.70)  {$h_3$};
\node[obox] (o4) at (5.30,-4.05)  {$h_4$};

\draw[->] (4.53, 0.00)  -- (o1.west);
\draw[->] (4.53,-1.35)  -- (o2.west);
\draw[->] (4.53,-2.70)  -- (o3.west);
\draw[->] (4.53,-4.05)  -- (o4.west);

% ── Legend (two rows, stacked vertically to avoid overlap) ───────────
\fill[teal!22]  (-0.48,-5.4) rectangle (0.18,-5.0);
\draw[gray!45]  (-0.48,-5.4) rectangle (0.18,-5.0);
\node[font=\footnotesize, anchor=west] at (0.28,-5.2)
  {Attends ($\alpha_{ij}$ computed normally)};
\fill[gray!14]  (-0.48,-6.0) rectangle (0.18,-5.6);
\draw[gray!45]  (-0.48,-6.0) rectangle (0.18,-5.6);
\node[font=\footnotesize, anchor=west] at (0.28,-5.8)
  {Masked to $-\infty$ before softmax};

\end{tikzpicture}
\caption{Causal self-attention mask for a 4-item sequence. Rows are query positions $(\mathbf{q}_i)$; columns are key positions $(k_j)$. Teal cells are allowed: position $i$ can attend to any position $j \leq i$. Grey cells are masked to $-\infty$ before softmax, preventing the model from attending to future items. The same input items $i_1$--$i_4$ serve as both queries (rows) and keys (columns).}
\label{fig:causal_attn}
\end{figure}

DIF-SASRec~\cite{xie2022difsasrec} extends SASRec by incorporating \textit{side information} — item attributes such as textual content and categorical metadata — through decoupled attention streams.
Rather than fusing attribute embeddings into item ID embeddings before attention (which conflates the two signals), DIF-SASRec maintains separate attention modules for content and category features and combines them at the output layer.
This decoupling preserves the ability to attend to content-relevant history and category-relevant history independently, improving performance on items with rich metadata.


\medskip
\noindent\textbf{Limitations of SASRec and BERT4Rec}


Although SASRec and BERT4Rec significantly advanced sequential recommendation performance compared with recurrent architectures such as GRU4Rec, both models retain limitations that reduce their suitability for large-scale real-time recommendation systems with rich side information.

SASRec replaces recurrent sequence modelling with a causal self attention Transformer architecture, enabling parallel training.However, the original SASRec design represents each item primarily through an item ID embedding. Consequently, semantic information such as textual descriptions, categories, or visual features is not explicitly modelled within the attention mechanism.

BERT4Rec addresses some sequence modelling limitations by introducing bidirectional self attention with a Cloze style prediction. Unlike SASRec, which predicts the next item autoregressively, BERT4Rec learns contextual representations using both left and right sequence context. Despite these advantages, BERT4Rec has several practical limitations for recommendation systems. First, the training objective is inconsistent with real time  recommendation. Second, BERT4Rec does not explicitly separate collaborative and content based signals within the attention architecture

Finally, both SASRec and BERT4Rec exhibit limited robustness in multilingual and multimodal recommendation environments. Their original formula rely primarily on item identifiers and do not natively incorporate multilingual semantic encoders or visual representations. These limitations motivate the adoption of DIF-SASRec, which introduces decoupled attention mechanisms for different feature modalities. By separating item, content, and category attention streams, DIF-SASRec enables more specialised modelling of heterogeneous recommendation signals.

The below figure compares the item representation learning process used in SASRecF, NOVA-SR, and DIF-SR. Unlike early-fusion approaches that combine all side information into a single representation before attention computation, DIF-SR decouples the attention generation process for different feature modalities and fuses the resulting attention matrices afterwards. This design improves representation flexibility and reduces interference between heterogeneous features.

\begin{figure}[H]
\centering
\includegraphics[width=1.1\textwidth]{Captsone/images/chapter2/compareItemPresLearning.png}
\caption{Comparison of item representation learning in SASRecF, NOVA-SR, and DIF-SR. Early-fusion approaches (left) merge all side information into a single embedding before attention; DIF-SR (right) decouples the attention computation for each feature modality and fuses the resulting attention matrices afterwards}
\label{fig:compare_item_pres_learning }
\end{figure}

\label{sec:difsasrec_arch}


\subsection{DIF-SASRec Architecture}
\label{sec:difsasrec_arch}
DIF-SASRec extends the SASRec sequential recommendation architecture by explicitly decoupling different side information modalities within the self attention mechanism. Instead of combining all item attributes into a single embedding representation before attention computation, DIF-SASRec maintains independent attention streams for content and category information. This design enables the model to capture heterogeneous behavioural patterns more effectively while preserving the parallel training advantages of Transformer based recommendation models.The below figure illustrates the overall architecture of the proposed DIF-SASRec network.

\begin{figure}[H]
\centering
\includegraphics[width=1.0\textwidth]{Captsone/images/chapter2/architectDecoupled.png}
\caption{Overall architecture of the DIF-SASRec network. Content and category side information are processed through independent multi-head attention streams; the resulting attention maps are fused via a learnable scalar $\alpha$ before being applied to the shared value matrix.}
\label{fig:dif_sasrec_architecture}
\end{figure}

\textbf{Embedding module.}
Each item $i_t$ in the interaction sequence is represented by three components: a pre-computed dense text embedding projected to the model's hidden dimension $d$; a learned category embedding for the item's genre; and a position embedding that encodes sequence order.

\textbf{Decoupled attention.}
Each DIF layer runs two independent multi-head attention streams over the same sequence.
The content stream produces attention weights from text embeddings; the category stream produces attention weights from genre embeddings.
Both streams share the same value matrix (drawn from text embeddings), so only the attention pattern, not the attended content, is decoupled.
The two attention maps are fused via a learnable scalar $\alpha$:
\begin{equation}
  \mathbf{A}^{\text{fused}} = \alpha\,\mathbf{A}^{\text{cat}} + (1 - \alpha)\,\mathbf{A}^{\text{content}}
\end{equation}
The causal mask ensures each position attends only to past positions.
After $L$ stacked DIF blocks, the hidden state at the last interaction position is used as the user's intent vector for next-item prediction.

\textbf{Training.}
The model is trained with a sampled softmax loss: the ground-truth next item is ranked against a fixed pool of sampled negatives.
An auxiliary category prediction loss ($\lambda = 0.1$) reinforces the category stream.
At inference time, each user click triggers one additional gradient step on the personal checkpoint, keeping the model current with the user's latest interactions.

\subsection{Graph-Based Behavioural Embedding: Cleora}
\label{sec:cleora}

Graph-based embedding methods represent items as nodes in a graph and learn dense vector representations that encode the graph's structural proximity.
In a co-purchase graph, items are connected by an edge if they are frequently bought together; the edge weight reflects the strength of that co-occurrence.
Dense items in tightly connected communities — such as books in the same series or by the same author — receive similar embeddings, enabling efficient candidate retrieval by nearest-neighbour search.

Cleora~\cite{rychalska2021cleora} is an unsupervised graph embedding scheme based on iterative feature propagation.
Starting from random node features, each iteration replaces every node's embedding with the weighted average of its neighbours' embeddings, followed by row-wise $\ell_2$ normalisation.
Formally, for node $v$ at iteration $t$:
\begin{equation}
  \mathbf{e}_v^{(t)} = \frac{\sum_{u \in \mathcal{N}(v)} w_{uv}\, \mathbf{e}_u^{(t-1)}}{\left\| \sum_{u \in \mathcal{N}(v)} w_{uv}\, \mathbf{e}_u^{(t-1)} \right\|_2}
\end{equation}
where $\mathcal{N}(v)$ is the neighbour set of $v$ and $w_{uv}$ is the edge weight.
Unlike DeepWalk or Node2Vec, Cleora requires no random-walk sampling and no contrastive objective, making it substantially faster and scalable to hundreds of thousands of nodes on a single CPU.
After a fixed number of iterations (typically two to three), the resulting embeddings are compact representations of co-purchase community membership.

\subsection{Dense and Sparse Retrieval with Hybrid Fusion}
\label{sec:retrieval}

\subsubsection{Bi-Encoder Dense Retrieval}

Dense retrieval represents both queries and documents as fixed-length vectors and retrieves candidates by computing the inner product (or cosine similarity) between query and document embeddings.
A \textit{bi-encoder} architecture uses two independent encoders — one for queries and one for items — allowing item vectors to be pre-computed and indexed offline.
At query time, only the query encoding and a vector lookup are needed, making dense retrieval highly efficient.

BGE-M3~\cite{chen2024bge} (\texttt{BAAI/bge-m3}) is the encoder used in this system.
It supports 100+ languages and produces 1{,}024-dimensional dense embeddings.
Its multilingual pre-training via self-knowledge distillation enables strong retrieval quality on both English and Vietnamese queries without language-specific fine-tuning.

\subsubsection{Approximate Nearest Neighbour Search}

Exact nearest-neighbour search over millions of vectors scales as $O(n \cdot d)$ per query, where $d$ is the embedding dimension.
For $n = 1{,}700{,}000$ vectors of dimension 1{,}024 this is prohibitively slow at interactive latencies.
Hierarchical Navigable Small World (HNSW)~\cite{malkov2018hnsw} graphs provide approximate nearest-neighbour search in $O(\log n)$ expected time.
HNSW builds a layered proximity graph: lower layers are dense with short-range edges and upper layers are sparse with long-range edges.
At query time, a greedy search descends from the sparser upper layers to progressively refine the candidate set, allowing large portions of the index to be skipped.
FAISS~\cite{johnson2021faiss} provides a GPU-accelerated implementation of both HNSW and flat (exact) indices used in this system.

\subsubsection{Sparse Retrieval and Reciprocal Rank Fusion}

BM25 is a probabilistic sparse retrieval model that scores a document $d$ for query $q$ as:
\begin{equation}
  \text{BM25}(q, d) = \sum_{t \in q} \text{IDF}(t) \cdot \frac{f(t, d) \cdot (k_1 + 1)}{f(t, d) + k_1 \left(1 - b + b \cdot \frac{|d|}{\text{avgdl}}\right)}
\end{equation}
where $f(t, d)$ is the term frequency of token $t$ in document $d$, $|d|$ is document length, $\text{avgdl}$ is the corpus average document length, and $k_1 = 1.2$, $b = 0.75$ are standard hyperparameters.
Tantivy, a Rust-native full-text search library, provides BM25 indexing and retrieval over the item catalogue.

Dense and sparse rankings capture complementary signals — dense retrieval finds semantically similar items even when the query shares no tokens with the document, while BM25 excels at exact-match term recall.
Reciprocal Rank Fusion (RRF)~\cite{cormack2009rrf} combines two ranked lists without score normalisation:
\begin{equation}
  \text{RRF}(d) = \sum_{r \in \mathcal{R}} \frac{1}{k + r_i(d)}
\end{equation}
where $\mathcal{R}$ is the set of input rankings, $r_i(d)$ is the rank of document $d$ in ranking $i$, and $k = 60$ is a smoothing constant.
RRF is robust to score scale differences between retrieval systems and has been shown to outperform learned rank aggregation methods in low-resource settings~\cite{cormack2009rrf}.
Figure~\ref{fig:rrf_fusion} illustrates how the two ranked lists are merged.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  >=Stealth,
  every path/.style={line width=0.65pt},
  listbox/.style={draw=gray!55, fill=white, rounded corners=3pt,
                  minimum width=3.4cm, align=left,
                  inner sep=7pt, font=\small},
  fusebox/.style={draw=orange!65, fill=orange!7, rounded corners=4pt,
                  thick, align=center, font=\small,
                  minimum width=7.2cm, minimum height=1.1cm, inner sep=8pt},
  outbox/.style={draw=teal!55, fill=teal!5, rounded corners=3pt,
                 minimum width=3.4cm, align=left,
                 inner sep=7pt, font=\small},
]

% ── Dense ranked list (left) ──────────────────────────────────────────
\node[listbox, anchor=north] (dense) at (-3.8, 2.0) {%
  \textbf{Dense (FAISS)}\\[3pt]
  \#1\enskip Item~A\\
  \#2\enskip Item~B\\
  \#3\enskip Item~C\\
  \#4\enskip Item~D
};

% ── Sparse ranked list (right) ────────────────────────────────────────
\node[listbox, anchor=north] (bm25) at (3.8, 2.0) {%
  \textbf{Sparse (BM25)}\\[3pt]
  \#1\enskip Item~C\\
  \#2\enskip Item~A\\
  \#3\enskip Item~E\\
  \#4\enskip Item~B
};

% ── RRF fusion box ────────────────────────────────────────────────────
\node[fusebox] (fusion) at (0, -4.0) {%
  \textbf{Reciprocal Rank Fusion}\\[4pt]
  $\mathrm{RRF}(d)=\dfrac{1}{k+r_{\mathrm{dense}}(d)}+\dfrac{1}{k+r_{\mathrm{bm25}}(d)},\quad k=60$
};

% ── Fused output list ─────────────────────────────────────────────────
\node[outbox, anchor=north] (merged) at (0, -5.6) {%
  \textbf{Fused Results}\\[3pt]
  \#1\enskip Item~A\quad $\tfrac{1}{62}+\tfrac{1}{62}=0.0323$\\
  \#2\enskip Item~C\quad $\tfrac{1}{63}+\tfrac{1}{61}=0.0323$\\
  \#3\enskip Item~B\quad $\tfrac{1}{62}+\tfrac{1}{64}=0.0317$\\
  \#4\enskip Item~E\quad $0+\tfrac{1}{63}=0.0159$
};

% ── Arrows ────────────────────────────────────────────────────────────
% Dense → Fusion: down then elbow into fusion.west
\draw[->] (dense.south) -- (fusion.north);
% BM25 → Fusion: down then elbow into fusion.east
\draw[->] (bm25.south) -- (fusion.north);
% Fusion → Merged
\draw[->] (fusion.south) -- (merged.north);

\end{tikzpicture}
\caption{Reciprocal Rank Fusion combining dense FAISS and sparse BM25 rankings. Each document's RRF score sums its reciprocal ranks across both lists; the constant $k=60$ prevents any single top-ranked item from dominating. Items absent from one list receive no contribution from that list (Item~E is absent from the dense list).}
\label{fig:rrf_fusion}
\end{figure}

\subsection{Multimodal and Multilingual Extensions}
\label{sec:multimodal}

\paragraph{Visual Search (CLIP) —}
CLIP~\cite{radford2021clip} is a vision-language model pre-trained via contrastive learning on 400 million image–text pairs.
It maps both images and text descriptions into a shared embedding space, enabling zero-shot image search: a query image can be encoded and matched against text-based item embeddings without domain-specific fine-tuning.
In the system, a CLIP HNSW index over 3 million item cover images enables cover-image-based book search.

\paragraph{Multilingual Query Handling (NLLB) —}
When a query arrives in Vietnamese, the NLLB-200 model~\cite{nllb2022} translates it to English before the BGE-M3 encoding step.
NLLB-200 is a model family supporting 202 languages, trained specifically to improve translation quality for low-resource languages; this system uses the distilled 600M-parameter variant (\texttt{facebook/nllb-200-distilled-600M}) for its latency characteristics.
Query-time translation enables the system to leverage the full semantic richness of the BGE-M3 English-dominant training distribution while still accepting Vietnamese input natively.

