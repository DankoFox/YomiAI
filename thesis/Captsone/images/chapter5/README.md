# Chapter 5 Figures

Generated by `notebooks/chapter4/generate_figures.py` (300 DPI, PDF + PNG).
All figures use real data from `evaluation/` and `profiling/` — no synthetic values.

---

## fig_dataset_statistics

**LaTeX caption:**
```latex
\caption{Distribution of training sequence lengths across 100{,}000 evaluation users.
(a) Histogram of sequence length buckets. (b) Percentile breakdown showing that the
median user has 312 interactions and the 90th-percentile user has over 800.}
```
**Referenced in:** §5.1 (Experiment Setup)

---

## fig_main_results

**LaTeX caption:**
```latex
\caption{Main evaluation results for all six systems (100{,}000 users, $N=99$ negatives,
seed=42). HR@10 (solid) and NDCG@10 (hatched) are shown side by side.
The proposed System ($A \cup B$) achieves HR@10 = 0.9736.}
```
**Referenced in:** §5.3 (End-to-End Evaluation)

---

## fig_n_sensitivity

**LaTeX caption:**
```latex
\caption{HR@10 under two negative sample sizes ($N=99$ and $N=999$) over 100{,}000 users.
The rank ordering of methods is preserved; horizontal dashed lines show the random
baseline for each protocol (0.100 and 0.010, respectively).}
```
**Referenced in:** §5.3 (N-Sensitivity)

---

## fig_multiseed_stability

**LaTeX caption:**
```latex
\caption{DIF-SASRec evaluation stability across five random seeds controlling negative
sampling (100{,}000 users, $N=99$ each). Red dashed line = mean; shaded band = $\pm\sigma$.
HR@10: mean = 0.7244, $\sigma$ = 0.0024. NDCG@10: mean = 0.4441, $\sigma$ = 0.0017.}
```
**Referenced in:** §5.3 (Multi-Seed Stability)

---

## fig_complementarity

**LaTeX caption:**
```latex
\caption{Pipeline complementarity analysis (100{,}000 users, $N=99$, seed=42).
(a) Donut chart of hit distribution: 70.6\% of users are served by both pipelines,
19.9\% by Pipeline A only, 6.9\% by Pipeline B only, and 2.6\% by neither.
(b) Absolute user counts.}
```
**Referenced in:** §5.4 (Qualitative Analysis)

---

## fig_encoder_query_comparison

**LaTeX caption:**
```latex
\caption{Search query retrieval Precision@10 for BGE-M3 (current) vs.\ BLaIR (legacy)
across five query-type groups. BGE-M3 substantially outperforms BLaIR on
Vietnamese queries (shaded region), where BLaIR achieves Precision@10 $\leq 0.05$.}
```
**Referenced in:** §5.2 (Component Analysis — Encoder)

---

## fig_hnsw_latency

**LaTeX caption:**
```latex
\caption{HNSW vs.\ flat index trade-off. (a) P50 and P95 search latency: HNSW
achieves a 5.12$\times$ median speedup (612 ms → 120 ms P50). (b) Mean recall@$k$
of the HNSW index: 0.521 (exact = 1.0).}
```
**Referenced in:** §5.2 (Component Analysis — HNSW)

---

## fig_search_pipeline_latency

**LaTeX caption:**
```latex
\caption{End-to-end search pipeline latency by optimisation stage (stacked bars).
The dominant baseline bottleneck (translation, 204 ms) was reduced to under 2 ms
through model quantisation and warm caching, achieving an 18.6$\times$ overall
latency reduction from 1{,}102 ms to 59 ms (warm cache).}
```
**Referenced in:** §5.2 (Component Analysis — Latency)
